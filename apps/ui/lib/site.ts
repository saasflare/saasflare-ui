/**
 * @fileoverview Canonical site constants — single source for the public origin
 * used by metadata, the sitemap, robots.txt, and OG image absolute URLs.
 */

/** Public origin of ui.saasflare.io (no trailing slash). */
export const SITE_URL = "https://ui.saasflare.io"

/** Site-wide name + tagline used in metadata and OG images. */
export const SITE_NAME = "Saasflare UI"
export const SITE_TAGLINE = "The AI-native React component library"
