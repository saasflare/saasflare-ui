/**
 * @fileoverview Fumadocs MDX collections — content discovery for ui.saasflare.io docs.
 * @module apps/ui/source.config
 */
import {defineDocs, defineConfig} from 'fumadocs-mdx/config';

export const docs = defineDocs({
    dir: 'content/docs',
});

export default defineConfig();
