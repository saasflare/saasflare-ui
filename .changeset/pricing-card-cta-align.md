---
"@saasflare/ui": patch
---

`PricingCard` pins its CTA to the bottom of the card. Plans rarely have the same number of features, so the buttons previously landed at whatever height the feature list ended — the one detail that makes an otherwise finished pricing table look unfinished.
